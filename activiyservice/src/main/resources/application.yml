spring:
  application:
    name: activity-service

  data:
    mongodb:
      uri: "mongodb+srv://abhishekmali7710_db_user:mummy123abhi@abhi01cluster.zbdjysk.mongodb.net/fitnessactivity?retryWrites=true&w=majority"
      database: fitnessactivity

    rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking