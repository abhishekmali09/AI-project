spring:
  application:
    name: api-gateway

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8181/realms/fitness-oauth2/protocol/openid-connect/certs
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**

        - id: ACTIVITY-SERVICE
          uri: lb://ACTIVITY-SERVICE
          predicates:
            - Path=/api/activities/**

        - id: AI-SERVICE
          uri: lb://AI-SERVICE
          predicates:
            - Path=/api/recommendation/**

        - id: ai-food-analysis
          uri: lb://AI-SERVICE
          predicates:
            - Path=/api/ai/**

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true      # add this
    register-with-eureka: true  # add this
  instance:
    prefer-ip-address: true
    hostname: localhost